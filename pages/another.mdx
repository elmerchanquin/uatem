# Another Page

```js filename="demo.js" {3} copy
let a = 1;

console.log(a);
```

El ordenador cuántico: una tecnología emergente que utiliza los principios de la mecánica cuántica para realizar cálculos mucho más rápidos que los ordenadores clásicos.

El telescopio espacial Hubble: un telescopio que se encuentra en órbita alrededor de la Tierra y que ha sido utilizado para observar el universo en gran detalle.

La Estación Espacial Internacional: una estructura habitable en órbita alrededor de la Tierra, que es utilizada para realizar investigaciones científicas en el espacio.

El cohete Falcon Heavy: un cohete diseñado y construido por la empresa SpaceX, que puede llevar grandes cargas útiles al espacio.

El automóvil eléctrico Tesla: un vehículo eléctrico avanzado que utiliza tecnología de vanguardia para ofrecer una experiencia de conducción más limpia y eficiente.



## Component

import { useState } from 'react'

{/* Import CSS modules */}
import styles from '../components/counters.module.css'

export const Counter = () => {
  const [count, setCount] = useState(0);
  return (
    <div>
      <button onClick={() => setCount(count + 1)} className={styles.counter}>Clicked {count} times</button>
    </div>
  );
};

<Counter/>

## External Component

import Counters from '../components/counters'

<Counters />
